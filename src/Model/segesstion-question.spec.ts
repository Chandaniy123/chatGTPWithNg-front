import { SegesstionQuestion } from './segesstion-question';

describe('SegesstionQuestion', () => {
  it('should create an instance', () => {
    expect(new SegesstionQuestion()).toBeTruthy();
  });
});
